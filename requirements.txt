# Multi-Line Object Counter - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
numpy>=1.23.0,<2.0.0
ultralytics>=8.0.0
torch>=2.0.0
pyyaml>=6.0
pandas>=2.0.0
openpyxl>=3.1.0
psutil>=5.9.0
pillow>=9.0.0

# Visualization dependencies
matplotlib>=3.7.0
seaborn>=0.12.0

# Cloud API upload dependencies (required when enable_api_upload=true;
# they are imported at module load so they must be installed unconditionally)
requests>=2.31.0
python-dotenv>=1.0.0

# Optional: ONNX runtime for .onnx models. onnxruntime is the primary
# path for detecting an ONNX model's static input shape; the onnx package
# is only used as a fallback if onnxruntime can't open the file.
# onnxruntime>=1.16.0
# onnx>=1.14.0

# Optional: memory profiling (enables the `--memory-profile` CLI flag)
# memory-profiler>=0.61.0

# Optional: TensorRT for NVIDIA GPU optimization
# TensorRT cannot be installed via pip - install manually from NVIDIA:
# https://developer.nvidia.com/tensorrt
# Use .pt (PyTorch) or .onnx models if you don't need TensorRT optimization

# Note: tkinter is required but comes bundled with Python on most systems
# If missing on Linux: sudo apt-get install python3-tk

